import React from 'react'

import { IconProps } from '../types'

const BarCampIcon = ({ size, ...props }: IconProps) => {
	return <img src="/static/images/Artwork/logo.png" alt="BarCamp logo" />
}

export default BarCampIcon
